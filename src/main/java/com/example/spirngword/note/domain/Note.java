/*
package com.example.spirngword.note.domain;

import com.example.spirngword.user.domain.User;
import jakarta.persistence.*;

@Entity
@Table(name="NOTE")
public class Note {
    @Id
    @GeneratedValue
    @Column(name="NOTE_ID")
    private Long id;

    @ManyToOne
    @JoinColumn (name="USER_ID")
    private User user;

    public Long getId() {
        return id;
    }

    public void setId(Long id) {
        this.id = id;
    }

    public User getUser() {
        return user;
    }

    public void setUser(User user) {
        this.user = user;
    }
}
*/
